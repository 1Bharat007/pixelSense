# Coding Standards

- **Rust**: We follow standard Rust idioms. Code must pass `cargo fmt` and `cargo clippy` without warnings.
- **TypeScript**: Use strict mode. Avoid `any`.
- **Architecture**: Follow Clean Architecture. Do not mix UI code with business logic.
